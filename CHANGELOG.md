# 版本记录

本项目是单校定制的 fork，版本从 1.0 起算，不继承上游的编号。

## 1.2

- **课表管理**：每张课表提供可见的操作按钮，支持删除当前课表及最后一张课表。删除前二次确认，
  有剩余课表时自动切换；没有课表时显示空状态，并清除周视图小组件中的旧内容。
- **课程通知**：一次性提醒与静默状态分开设置。状态通知持续显示距下一节有效课程的小时和分钟，
  支持跨无课日、单双周和跨午夜课程；上课、下课提醒分别设置提前量，仅合并同刻触发的提醒。
  过期提醒不补发，隐藏本次后不重新显示该次课程。
- **Android 实时通知**：自动模式在课程进行中请求 Android 16 原生实时通知，不使用厂商焦点协议。
  不支持或未获实时通知授权时保留标准通知栏状态；全部通知被系统禁用时不发送通知。
- **小组件**：保持现有风格，调整 2×2、4×2、5×2、4×4 等尺寸的信息布局，优先保留课名、时间和地点。
  课前状态保持蓝色，超过 60 分钟显示一位小数小时，60 分钟以内显示具体分钟，不再显示“待上课”。
  周视图按实例宽度绘制，改进重叠课程与大字体显示，纵向内容可滚动。
- **设置与加载**：精简设置文案，区分时间栏、课程作息和通知作用范围；移除无效的小组件标题颜色选项
  及额外的页面预加载开关。保留 ViewPager 最低邻页缓存，避免重复初始化和重复注册观察者。
- **刷新与维护**：纯显示刷新不主动唤醒休眠设备，课程边界保留独立唤醒闹钟；小时文字仅在显示值
  变化时刷新。修复仅添加 2×2 小组件时的倒计时调度，清理死代码和未使用资源，修复 5 项 Lint 错误。
- **独立测试版**：新增 `simulation` 变体，支持日期定位、时间拖动、暂停及 0.25 至 120 倍速。
  测试数据与普通版隔离，不修改系统时间；普通版不包含测试控制页或计时服务。

兼容说明：最低 Android 5.0，`targetSdk 29` 不变，`compileSdk` 升至 36，无新增第三方依赖。
升级保留通知偏好，原常驻开启迁移为“仅通知栏”；新安装默认关闭通知总开关和下课提醒。
实时通知呈现、后台提醒及小组件尺寸受系统和桌面限制；Android 14 及以上允许用户主动划走状态通知。

## 1.1

- **静态检查修复**：作息编辑页观察者随视图销毁移除，避免返回编辑页后重复刷新旧列表；
  首页周次选择与滚动改为按页面、布局状态同步，移除固定一秒等待；登录页证书验证失败时
  停止连接并显示原因，不再提供“继续浏览”入口。

- **后台设置**：完成 HyperOS 无限制设置后，再次点击可进入 Android 电池设置；未获豁免时
  打开授权页，已获豁免时打开优化列表。精简设置行说明为「减少待机时提醒延迟」。

- **启动与课表加载**：修复 Room 尚未首次查询时重复创建数据库实例的问题；七天课程与
  预加载周的空状态共用一次整表观察查询，数据未变化的日期不再重复重绘。保留邻周预加载、
  课程修改后的自动刷新和原有单双周显示规则，不改变课表数据结构或提醒排期。

- **SUES 导入**：账号密码页提交登录后立即显示遮罩等待跳转；进入独立的滑动登录页后，
  等滑块就绪才在本机识别缺口并代拖，每份页面最多试 3 次。识别失败、达到次数上限或等待
  滑块超过 20 秒时撤罩并提示手动验证；被退回账号密码页时撤罩，方便修改后重新提交。
  识别在本机完成，验证码图片不出设备；账号密码仍全程由页面自己加密，App 不经手。
  算法契约与选型数据见 [docs/EAMS-IMPORT.md](docs/EAMS-IMPORT.md)。

## 1.0.2

- 修复小部件浅色底板配浅色文字的问题：表头颜色在桌面解析，课程位图跟随系统主题刷新。
- 统一应用页面的浅深色文字对比、卡片和表单圆角；课程管理卡片支持两行课名与自适应高度。
- **空状态崩溃**：修复全新安装（或删光课表）后首页依赖课表的入口一点即崩的问题——右下角「+」
  加课、顶栏日期行（周数面板）、分享导出、底部面板的「修改当前周 / 上课时间 / 更换背景 /
  已添课程」、抽屉「课程管理」、侧栏表列表原先都在课表未就绪时裸读 `lateinit`。现在统一走
  `requireTable` 闸门：没有课表时提示先导入或新建，加载中提示稍候，就绪后行为不变
  （`EmptyStateEntryGuardTest` 钉住空状态不崩、有课表照常放行两条路径）。

- 设置页支持直接申请添加日视图、周视图；周视图先选课表，系统确认后绑定实例。
- 小部件选择器改用完整课程示例预览，修复只有表头和空白区域的问题。
- 重做课程详情为简洁的单层面板；重叠课程可通过箭头或列表切换，按实际时间判断重叠。
- 删除操作改为显式选择安排范围并确认，替代长按直接删除整门课。

- **小组件**：删除 4×2 右上角「第 N 周」蓝色药丸，日期与切换箭头各占其位。
- **关于**：移除「上海工程技术大学」徽标——本项目没有学校背书。
- **关于**：文案与排版改为书面口径，并减少栏目——三张卡片（应用信息 / 使用说明 / 权限说明）
  加页脚署名，全页不再画分隔线；应用信息卡补一行可点的项目地址。
- **侧栏**：删掉「新建课表」（与底部面板那个按钮是同一件事），「关于」挪到抽屉最下方并简化成
  页脚——不再用导航行的圆底图标与强调色胶囊。
- **首次启动**：修复「先弹没有课表、再弹关于本软件」——两个弹窗原先各挂在一个计时器上，
  谁先到谁先弹；顺序改由状态决定，声明讲完才轮到导入引导。
- **署名口径**：首次启动的声明、`NOTICE`（随包）与 `README` 里的校名一律改成「**适配**上海
  工程技术大学的教学系统」，并写明本项目是个人独立开发、与该校无隶属或合作关系、未获其
  授权或背书、非该校官方应用——出现校名只为说明适配对象。
- **设置**：「后台运行」一行改为固定名称 + 状态值，去掉厂商专属说法（HyperOS/AOSP 字样），
  失败提示改成可照做的说法；修复每次回到设置页那一行会脱离分组、单独显示成圆角卡片的问题
  （返回时刷新把分组位置这类派生状态丢了）。
- **设置**：「显示主题」右侧上下箭头此前过扁（斜率约 32°、两个尖几乎贴在一起），
  现在按 45° 重画并留出可见缝；这条几何抽成了可断言的常量，避免再次退化。
- **设置**：修复 Android 5–9 点「显示主题」/作息时间表弹列表时的 `NoSuchMethodError`
  崩溃——弹窗背景内边距改用 `InsetDrawable`（API 1），不再调用 API 29 才有的
  `GradientDrawable.setPadding`。
- **设置**：修复 Android 5.0/5.1 打开设置页直接崩溃的问题——「后台运行」那一行的状态判断会调用
  API 23 才有的 Doze 白名单查询，现在带上版本守卫（老机器显示「未设置」），并把这条从 lint
  基线里撤出，不再被基线掩盖。
- **导入**：修复自动导入页「登录成功后直接返回」的 `NullPointerException`——门户探测是自续的
  定时回调，而 `postDelayed` 投出的消息不会随页面销毁消失；现在离开时取消它，并且所有可能在
  视图销毁后才触发的回调（探测、JS 回调、进度回调、SSL 错误）都先确认视图还在。
- **导入**：修复「用其他 App 打开 .wakeup_schedule」在参数不完整时崩溃的问题（缺 `url` 直接
  断言）；现在给出提示并结束，不再留下一个空白页或一次崩溃。
- **导入**：分享文件加上 1 MB 上限——文件类型只看文件名，此前一个几百 MB 的同名文件会被整个
  读进内存。
- **清单**：带 intent-filter 的组件全部显式声明 `android:exported`（`SplashActivity`、
  `LoginWebActivity`、周视图小部件配置页、两个 `AppWidgetProvider`），取值就是原来的默认值，
  行为不变；targetSdk 升到 31+ 时缺这一行会直接构建失败。

### 交互（本轮）

- **小部件**：**移除应用内「添加桌面小部件」入口**（`设置 → 外观` 里的那一行），
  小部件改为只从桌面的小部件列表添加。该入口在各个桌面下都走不通：厂商桌面要么不支持
  `requestPinAppWidget`，要么（HyperOS 实测）接受请求却既不弹确认框、也不真的落一个实例，
  最后都是把用户丢进第三方列表里自己找。相关代码（`WidgetPinReceiver`、厂商
  `widget://picker` 私有 intent、`.utils.WidgetPinReceiver` 清单声明）一并删除，不留死代码。
- **触感**：新增统一的 `Haptics` 入口（轻点 / 切换 / 长按三档，全部走 `performHapticFeedback`
  的平台语义常量，用户关掉系统触感时不会震），并铺到课表页圆钮、课程格子、周切换、课程详情、
  导入与导出面板等主要点击上；程序化刷新（冷启动定位当前周、展开面板对齐周次）不发回执，
  避免"什么都没点却震了一下"。
- **分享/导出**：修复导出面板退不出去——面板此前锁着不可取消，返回手势与点遮罩都被无视，
  只剩下最底下那个「取消导出」；现在返回键/手势、点面板外、点取消三条路都通。
  导出完成后的「是否分享」对话框同样不再锁定。
- **导入**：用其他 App 打开 `.wakeup_schedule` 失败时不再停在一个空白页上，而是提示后结束自己；
  完全没有可识别参数被外部拉起时同样不会留在空白页。导入前的文件选择器提示框也改为可取消。
- **课表**：全新安装不再预置一张空课表——首次进入是**无课表**状态（首页据此引导导入），
  不再是"一张没有名字的空课表"（此前它和"用户把课表删光了"完全无法区分）。
  库里一张表都没有时，用户新建的第一张表自动成为默认表，建完即可在首页看到。
- **侧栏**：抽屉里五行（我的课表 / 课表管理 / 设置 / 关于 / 新建课表）点击都补上触感回执。
- **返回**：关于、设置、课表管理等页面左上角返回箭头此前没有任何触感（三个基类都没接），已补齐。
- **导入**：修复「用其他 App 打开课表备份」被误拒——入口自己抄了一份"只看 URI 路径"的判定，
  而文件管理器/分享给的是 `content://…/document/1234` 这种路径（文件名只在显示名里）。
  现在入口与导入器共用同一个函数，`file://` 则按真实文件名严格判定。
- **导入**：修复被误拉起时"整个 App 被清掉重启"——"不是课表备份"那条分支原本调
  `backToSplash()`（`CLEAR_TASK | NEW_TASK`），用户看到的是闪退加重启；现在只结束自己、回到来的地方。
  没有任何可识别参数的裸拉起改为静默结束（不再弹"导入参数不完整"）。
- **导入**：文件打不开时不再把系统原文（`open failed:ENOENT (No such file or directory)`）弹给用户，
  统一成人话「读取文件失败：文件打不开或已被移动」。
- **清单**：`LoginWebActivity` 的 `intent-filter` 删掉不生效的 `pathPattern`（没有 `scheme` 时它不参与
  匹配，实测连不带 data 的 ACTION_VIEW 都能解析到该 Activity），并注明真正的判定在 Activity 里。
- **侧栏**：修复"先放完抽屉收起动画、再放进入下一页面的动画"——原来写死等 360ms 让两者串行，
  现在抽屉不做收起动画、目标页面立刻启动，屏幕上只剩一次转场（实测 472ms → 约 175ms，
  其中约 90–120ms 是 adb 往返开销）。顺带挡住由此暴露的一处竞态：课表还没从库里读出来时
  点「课表管理」会给一句"还在加载"而不是读未初始化的 `lateinit`。
- **提示（Toast）**：删掉退后台时那条「对小部件的编辑需要按「返回键」退出设置页面才能生效哦」
  ——它在 App 离开前台后才弹出，被系统按"后台 toast"渲染成横贯屏幕的系统样式（这就是
  「占满 x 轴」的来源），且内容无用；App 内从此没有任何后台弹 toast 的路径。
- **提示（Toast）**：toast 背景改为 85% 半透明（Toasty 的五档颜色全部带 alpha，文字保持
  全不透明），不再是压在课表上的一块死实心色。
- **深色模式**：全面审计了深浅两套主题的取色（页面/卡片/文字/小部件/抽屉/WebView 遮罩/
  Toast 对比度），未发现失配；顺带删除六个全项目零引用的遗留资源（旧小部件白色底板 shape
  等——它们若还活着就是深色白底 bug，实际从未被渲染）。
- **设置页**：清掉全部「回到课表就生效」「重启App后生效」一类弹窗（Material Snackbar，
  全宽不透明——上一轮改透明度的是 toast，这一族没动过，所以"看起来没变化"）。
  开关本身的状态变化就是回执；确实要等的（重启、切页面、下一次提醒），把时机写进行副标题
  常驻显示，不再逐次弹窗。时间表管理的「删除成功~」同删（行消失即回执）。

### 小部件（HyperOS 规范）

- **4×2 / 4×4 重排版**：底板、圆角、安全区、文字层级对齐 HyperOS 小部件设计规范；
  禁止列表内滚动——放不下的课程不进列表，剩余高度均摊到各行槽位。
- **头部日期**改为「17日」；正在上/下一节色卡时间写成起止区间（如 `08:15-09:35`）。
- **色卡出图**：按 `measuredHeight` 绘制，修复第二行 `topMargin` 导致的蓝框底部截断；
  `getViewBitmap` 同步计入子 View margin。
- **颜色**：次要文字 60% 透明度；深色主文字 90% 白；底板 `#FFFFFF` / `#1A1A1B`。

### 小部件（选择器预览与点击）

- **选择器详情页点预览不再打开应用**：三个 provider 去掉 `android:previewLayout`，只保留
  `android:previewImage`，并删除三个只服务于它的预览布局。MIUI/HyperOS 的
  「添加小部件 → 点条目文字 → 详情页」会把预览布局用 `AppWidgetHostView` 托管起来，**并给预览
  控件挂一个"打开本应用"的点击**：点一下预览图就被带回主界面，看着像误触。真机证据：详情页
  该节点 `click=true`、内容描述来自预览布局（全工程只有 `day_widget_preview.xml` 写
  「当天课程示例」），它的 bounds `[372,752][848,1209]` 与启动日志里的 `bnds` 完全一致，而
  启动方是桌面（`com.miui.home`）用 **LAUNCHER 意图**发起的——与小部件自身的点击入口无关。
  只给位图时详情页退化成普通 `ImageView`、不挂点击，与哔哩哔哩等第三方小部件表现一致。
- **预览图浅色/深色各一套**：`previewImage` 是按 `@drawable` 引用解析的，会走 `-night`；此前
  深色模式命中的是一张旧版式残留图（还带着已删除的 `›` 与"下一节"行）。现在两套图都由
  `WidgetPreviewImageTest` 从真实布局渲染。
- **表头计数**改为 `3节 · 已上1节`（原来 `已上1` 不带单位）；表头的时刻与正文统一到同一个
  `now`（`refreshTodayWidget` 新增带默认值的参数），修复预览图里「3节 · 已上3节」配
  「正在上课」这种自相矛盾。

## 1.0.1

一轮全量 bug 排查后的修复，分析与逐条验收方法见
[docs/BUGFIX-MANUAL.md](docs/BUGFIX-MANUAL.md)。

### 修复

- **导出**：`.wakeup_schedule` 与 ICS 的输出流在写完后关闭——此前不关闭，云同步目录下
  可能得到空文件或半截文件，且每次导出泄漏一个文件描述符。
- **导入**：分享文件里缺失的 `room`/`teacher`/`color`/`courseName` 字段入库前归一为空串，
  修复畸形分享文件导致课表崩溃或显示 "@null"；残缺文件现在给出可读的失败原因而不是崩溃。
- **课程颜色**：解析收口到单一出口，畸形颜色串退回兜底色，不再崩掉课表或整个小部件。
- **课表管理**：删除课表先弹确认框；删掉默认课表时默认标记自动转移到剩余课表
  （此前删掉默认表后首页、提醒、日视图小部件全部失效，哪怕其他课表还在）。
- **时间表管理**：被课表引用的时间表不可删除并说明原因（此前外键静默把课表改指默认
  作息，上课时间全变）；拖动「每节时长」不再把第 12~30 节占位行改写成本 `00:45` 之类的
  脏数据；`23:30 + 50 分钟` 跨天得到 `00:20` 而不是 `00:00`；`8:00` 这类无前导零的时间
  不再引发崩溃。
- **周次计算**：开学日期不在周首日（如周三开学）时，从第二周起周次不再少算 1。
- **全新安装**：默认课表/作息播种改为幂等，播种失败不再被吞掉，会保留标记在下次启动重试。
- **提醒**：设置页与系统广播里的重排挪到后台线程执行，不再占用主线程。
- **其他**：ICS 导出文件名带 `.ics` 扩展名；周视图小部件配置页对失效实例判空；
  尚未开学时课表/小部件头部日期不再显示未来一周；「当前周」设置在搜索过滤下不再崩溃；
  首页七天课程观察者不再随设置页往返而堆积。
- **ICS 导出（续）**：单门课写入失败不再静默跳过——导出完成后会列出「没有写入日历」的
  课程；周日的课在英文 locale 设备上不再整周偏移。
- **小部件点击**：六处 PendingIntent 统一声明可变性（targetSdk 升到 31+ 的前置条件）。
- **发布包日志**：保留 i/w/e 级日志只删 v/d——「重排完成」这类排障自证日志重新在正式包
  里可见。
- **课程数据异常提示**：点击课程崩溃时给出具体原因与处理路径，替换原先一句
  「哎呀>_<差点崩溃了」。
- **导入类型校验**：分享文件识别同时看 URI 路径与显示名，下载提供方等路径不含文件名的
  合法文件不再被误拒；两者都拿不到时交给解析阶段报错。
- **打包与构建**：移除无原生代码却声明的三套 ABI 过滤；导出 Room schema 留档
  （`app/schemas/`）；删除 R8 静默忽略的 ProGuard 专属指令；移除无 androidTest 源码集的
  instrumented 测试死依赖；lint 启用基线机制（存量问题入 `lint-baseline.xml`，
  新增问题挡在提交前）；协程收口到进程级 `appScope`。
- **作息对账**：新增测试把 App 侧导入作息逐格钉在学校公布的作息上，防止与 coursetime
  模块两处实现漂移。

## 1.0.0

首个版本。

### 功能

- **从教学服务中心导入**：引导完成学校 WebVPN 与统一身份认证，登录后自动进入课表页，读回
  本学期的课程、作息时间与学期起止日期。凭据由用户本人输入，应用不读取、不保存。
- **从导出文件导入**：读取本应用导出的 `.wakeup_schedule` 文件，也响应系统「打开方式」。
- **分楼作息**：上午第 3~5 节按教学楼错峰，每条安排按自己所在楼宇取值。
- **真实时间轴**：课块按开始时刻与时长定位，同一时段重叠的课程并排显示。
- **桌面小部件**：一周课程、当天课程两种，含下课倒计时。
- **上课提醒**：提前分钟数可设，重启与改时间后自动重排，支持 Doze 白名单。
- **多课表管理**、手动增删改课程、自定义颜色与背景、自定义时间表。
- **导出与分享**：`.wakeup_schedule` 与 ICS 日历文件。
- **外观**：浅色 / 深色 / 跟随系统，主题色。

### 与上游的差异

以 [WakeupSchedule_Kotlin](https://github.com/YZune/WakeupSchedule_Kotlin)
（直接上游 [Cerbur/WakeupSchedule_Kotlin](https://github.com/Cerbur/WakeupSchedule_Kotlin)，
commit `a69fb75`）为基线：

- 删去 SUES 之外的全部导入路径及其解析器、Activity、布局与资源。
- 新增 SUES 导入器，覆盖 WebVPN 与统一身份认证的引导流程。
- 作息模型由「一张扁平时间表」改为按 `(节次, 分组)` 取值，分组由导入器按楼宇写入。
- 课表渲染改为真实时间轴。
- 移除捐赠与付费解锁入口、更新检查、上游服务客户端、App Center 统计 SDK、无关应用推广与
  上游客服链接。
- 应用改名，全部美术资源重绘（启动图标、通知图标、侧边栏图标、启动页 logo、小部件预览、
  空课表与网络错误插图）。
- 主界面默认背景照改为纯色（浅色纯白、深色纯黑）。
- 新增首次启动提示：上游来源与版本、Apache-2.0 条款、完全免费声明；许可证与 NOTICE 随包
  附带。

### 仓库

- 包名与命名空间为 `courseclock.timetable`。
- 目录按「Android 应用 / 作息换算模块 / 工具 / 设计源 / 文档」拆分，见
  [README](README.md#仓库结构)。
- 许可为 [Apache License 2.0](LICENSE)，版权与改动说明见 [NOTICE](NOTICE)。
