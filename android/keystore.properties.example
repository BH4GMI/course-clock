# 发布签名配置模板。
#
# 复制成 keystore.properties 再按本机情况填写。真正的 keystore.properties 已被
# .gitignore 排除，不会进入版本库。
#
# 口令不写在这里，也不写进任何文件：先设置环境变量 WAKEUP_KEY_PASSWORD，再运行
# gradlew assembleRelease。没有提供口令时构建会明确停止，不会产出未签名的包冒充发布包。
#
# 密钥库里只有一个私钥条目时可以不写 keyAlias，构建会自动读出；有多个条目则必须写明。

# 密钥库的绝对路径。Windows 上用正斜杠或双反斜杠，避免被 properties 当成转义符。
storeFile=C:/path/to/your-android-keystore.jks

# 私钥条目别名
keyAlias=your_key_alias
